# Anthropic API Key for Claude Code integration
ANTHROPIC_API_KEY=sk-ant-...

# Database path (optional, defaults to ~/.disco/disco.db)
# DISCO_DB_PATH=file:~/.disco/disco.db

# Server port (optional, defaults to 3030)
# PORT=3030
