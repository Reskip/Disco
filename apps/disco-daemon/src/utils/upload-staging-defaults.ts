/** Zero means that valid uploaded attachments do not expire automatically. */
export const DEFAULT_UPLOAD_TTL_MS = 0;
/** No practical Disco application-layer ceiling; disk/proxy limits still apply. */
export const DEFAULT_UPLOAD_MAX_BYTES = Number.MAX_SAFE_INTEGER;
